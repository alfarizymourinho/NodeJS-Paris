datasiswa = (["Razwa", "Paris", "Ryan", "cindy", "layla"]);
datasiswa.forEach(siswa => {
    console.log(siswa);
});

console.log("=========");
for (let i = o; i < datasiswa.lenght; 1++) {
    console.log("Siswa ke- " + i + " : " + datasiswa[i]);
}